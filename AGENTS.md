# 本地测试版本管理

- 用户要求本地测试目录只保留最新一套 EXE。每次新测试版构建并通过自检后，删除该目录内全部旧测试版本，不保留旧版本的设置、工作区或分析结果。
- 当前本地测试目录为仓库同级的 `本地测试`。清理前解析并核对绝对路径，只删除该目录内旧测试版本；保留刚验证的新版本，不清理照片源目录或其他工作区。
- 本地测试包与 GitHub Release 分开管理。推送源码不代表发布新的 Release，发布版本按用户要求执行。
