请在这台电脑本地执行本文件夹里的4070测试包。先读 README.txt 和 code/experiments/gpu_raw/README.md。
使用测试包自带 photos 文件夹中的同一批80张RAW。

使用自带 .\runtime\python.exe，不安装或替换系统 Python，不改算法、阈值或正式EXE。
执行 .\runtime\python.exe -X utf8 .\run.py --rounds 2 --workers 4。
脚本已包含自检和六次顺序对照，不要额外并行运行测试；仅使用1～80张RW2，不调用任何AI API。
若路径不存在，问我实际路径；如果自检、OpenCL或源文件检查失败，停止并说明具体原因，不绕过检查。
照片只读，保留旧工作区。所有新结果写在测试包results里。不要上传GitHub或自动更新驱动。
完成后读取 summary.json 和 summary.md，比较CPU、GPU单队列、GPU双队列的耗时、
内存/显存、真实kernel重叠、判定差异和CPU复查错误，告诉我是否有实际收益。
最后给我本次 feedback.zip 的完整路径，方便发回原电脑分析。
